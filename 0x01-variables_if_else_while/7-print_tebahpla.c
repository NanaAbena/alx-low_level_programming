#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char l;
for (lr = 'z'; l >= 'a'; l--)
putchar(l);
putchar('\n');
return (0);
}
