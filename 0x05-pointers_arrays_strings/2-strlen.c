#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: checks for the length of a string
 * Return: 0 on a success.
 */
int _strlen(char *s)
{
	int a = 0;

	for (; *s++;)
		a++;
	return (0);
}
